<% if @index %>
  $("#select_country<%= @index%>").html("<%= escape_javascript(options_for_select(@countries.pluck(:name, :id))) %>") 
<% else %>
$("#question_country_id").html("<%= escape_javascript(options_for_select(@countries.pluck(:name, :id))) %>") 
<% end %>

<div class="container mt-5 col-10">
  <h2 class="text-center">お知らせ一覧</h2>
</div>
<div class="container mt-5">
  <div class="card col-10">
    <div class="card-body">
      <div class="row">
        <div class="col-12 mx-auto">
          <ul class="list-group">
            <% if @notifications.count != 0 %>
              <% @notifications.each do |n| %>
                <%= link_to notification_path(n.id), method: :patch, class: "list-group-item list-group-item-action" do%>
                  
                  <p class="mb-0"><%= n.message %></p>
                  <% if n.is_read %>
                    <p class="text-right text-muted mb-0">既読</p>
                  <% else %>
                    <p class="text-right text-muted mb-0">未読</p>
                  <% end %>
                  
                <% end %>
                <p class="text-right text-muted"><%= " #{time_ago_in_words(n.created_at)}前" %></p>
              <% end %>
              <%= paginate @notifications%>
            <% else %>
              <div class="text-center text">お知らせはありません</div>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
function read(n){
   notification_read(n) 
}
</script>